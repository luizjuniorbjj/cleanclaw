[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn xcleaners_main:app --host 0.0.0.0 --port ${PORT:-8003}'"
healthcheckPath = "/health"
healthcheckTimeout = 45
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
